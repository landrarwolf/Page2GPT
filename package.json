{
  "name": "page2gpt",
  "version": "0.2.0",
  "private": true,
  "description": "Extract the current webpage into clean Markdown and send it to ChatGPT.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check extension/shared.js && node --check extension/content.js && node --check extension/popup.js && node --check userscript/Page2GPT.user.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
